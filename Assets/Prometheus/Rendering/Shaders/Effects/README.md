# Effect shaders

Project-owned alpha, additive, refraction, depth-fade, and atmospheric effect shaders belong here. Effects that sample scene color use `_CameraOpaqueTexture`; depth-aware effects use `_CameraDepthTexture`.
