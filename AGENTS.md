# 项目工作指令（prometheus）

以下是本项目的全局工作约定。请在所有会话中始终遵循。

## 代码风格

- 生成的所有代码都必须带上完善、清晰的注释（解释意图、关键参数、边界情况）。
- 代码尽量保持单行；仅当语法要求（如长链调用、多行字符串、超过约 100 字符的语句）或可读性确实需要时才折行。不要刻意为了"单行"而牺牲可读性。
- 生成答案时，请给出相关文件的引用（如 `Assets/Scripts/xxx.cs` 或相对路径），便于快速跳转定位。

## 文档维护

- 如果你修改了一个成型系统的链路逻辑，必须根据改动同步修补该系统的 Markdown 文档。
- 如果系统中找不到对应的文档，则新建一个，使用中文编写。
- 文档应反映改动后的实际行为，避免与代码脱节。

## 代码安全与逻辑

- 在理论上不应该出现逻辑错误的地方，不要额外添加安全兜底（避免用防御式代码掩盖本应成立的假设）。
- 未经明确要求，不要进行测试；默认只保证代码编译通过。

## 协作方式

- 在开始生成代码前，可以指出提示词中让你感到迷惑或可以优化的点。
- 如果对某个需求感到不确定或不安，可以选择不生成代码，停下来向开发者说明意见并询问下一步指示。
